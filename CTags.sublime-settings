{
	// Alter this value if your ctags command is not in the PATH, or if using
	// a different version of ctags to that in the path (i.e. for OSX)
	"command": "/usr/local/bin/ctags",
	"file_exclude_patterns": [".tags", ".log"]
}
